@model TicketingSystem_DotNetMVC.Controllers.RegisterViewModel
@{
    ViewData["Title"] = "Register";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register - Ticket System</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <style>
        body {
            min-height: 100vh;
            margin: 0;
            background:
                radial-gradient(circle at 8% 8%, rgba(79, 70, 229, 0.18), transparent 35%),
                radial-gradient(circle at 92% 92%, rgba(6, 182, 212, 0.18), transparent 30%),
                #f9fafb;
            display: grid;
            place-items: center;
            font-family: "Inter", "Segoe UI", sans-serif;
        }

        .auth-card {
            width: 100%;
            max-width: 500px;
            border: 1px solid #e5e7eb;
            border-radius: 16px;
            box-shadow: 0 18px 38px rgba(17, 24, 39, 0.12);
            overflow: hidden;
        }

        .auth-card .card-body {
            padding: 2rem;
        }

        .form-control {
            border-radius: 12px;
            min-height: 46px;
            border: 1px solid #d1d5db;
        }

        .form-control:focus {
            border-color: rgba(79, 70, 229, 0.65);
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.12);
        }

        .btn-primary {
            background: #4f46e5;
            border-color: #4f46e5;
            border-radius: 12px;
            min-height: 46px;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="card auth-card">
        <div class="card-body">
            <div class="text-center mb-4">
                <div class="fs-4 fw-bold">Create Account</div>
                <div class="text-secondary">Register for Ticket System</div>
            </div>

            <form method="post" asp-action="Register" asp-controller="Account">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="FullName" class="form-label">Name</label>
                    <input asp-for="FullName" class="form-control" placeholder="Enter full name" />
                    <span asp-validation-for="FullName" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Email" class="form-label">Email</label>
                    <input asp-for="Email" class="form-control" placeholder="Enter email" />
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Password" class="form-label">Password</label>
                    <input asp-for="Password" type="password" class="form-control" placeholder="Create password" />
                    <span asp-validation-for="Password" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="ConfirmPassword" class="form-label">Confirm Password</label>
                    <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="Confirm password" />
                    <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                </div>

                <button type="submit" class="btn btn-primary w-100">Register</button>
            </form>

            <div class="text-center mt-3">
                <a href="/Account/Login" class="text-decoration-none">Back to login</a>
            </div>
        </div>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>
